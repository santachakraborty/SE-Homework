var char1= 2;
// var lCase=char1.toLowerCase();
if(lCase<=1 && lCase>=9){
   console.log(char1+' is a number.')
}
else if(lCase==='a' || lCase==='e' || lCase==='i' || lCase==='o' || lCase==='u'){
  console.log(char1 + ' is a Vowel');
}else{
  console.log(char1 + ' is a Consonant');
}