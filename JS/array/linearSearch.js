
// let isFound=0;
let index=0;

function linearSearch(array,number){
  for(let i=0; i<array.length; i++){
    if(array[i]===number){
      // index=i;
    //  isFound=1;
        return 0;
    //  break;
    }
  }

  // if(isFound==1){
  //   return ' found in location ' + (index+1);
  // }else{
  //   return ' not found.';
  // }
}
